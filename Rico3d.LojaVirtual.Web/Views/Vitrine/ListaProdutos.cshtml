@using Rico3d.LojaVirtual.Web.HtmlHelpers
@model Rico3d.LojaVirtual.Web.Models.ProdustosViewModel


@foreach (var p in Model.Produtos)
{
    <div class="well">
        <h3><strong>@p.Nome</strong></h3>
        <h3><span class="pull-right label label-primary">@p.Preco.ToString("c")</span></h3>
        <h3><span class="lead">@p.Descricao</span></h3>
    </div>
    
    
    
    
}

<div class="btn-group pull-right">
    @Html.PageLinks(Model.Paginacao, x => Url.Action("ListaProdutos", new { pagina = x }))
</div>


